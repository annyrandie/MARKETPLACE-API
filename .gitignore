node_modules/
dist/
.secrets/
spec.json
*.log
.env
.env.*
!.env.example
# `secrets/*` (not `secrets/`) so the !negation below still works — git
# doesn't descend into an already-excluded directory to re-include a file.
secrets/*
!secrets/.gitkeep
.DS_Store
